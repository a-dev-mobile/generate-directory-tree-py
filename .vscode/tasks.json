{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Setup Virtual Environment and Install Requirements",
            "type": "shell",
            "command": "cd .venv/Scripts && source activate && cd ../.. && pip install -r requirements.txt",
            "problemMatcher": [],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
